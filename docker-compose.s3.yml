# batlehub — full development stack with RustFS (S3), Authentik (OIDC), and Jaeger
#
# Standalone compose file; bring it up with:
#   task compose:s3:up
#
# To start only the infrastructure for local cargo run:
#   task compose:s3:db
#
# ── Rootless Podman prerequisite ─────────────────────────────────────────────
# netavark (default kernel-bridge backend) tries to write to
# /proc/sys/net/ipv4/ping_group_range and fails in rootless user namespaces.
# Switch to pasta (pure user-space, no sysctl required) once:
#   task compose:s3:preflight
# That writes ~/.config/containers/containers.conf and runs podman system migrate.
#
# Exposed ports:
#   9000  — Authentik UI  (akadmin / akadmin)
#   9900  — RustFS S3 API
#   9901  — RustFS console UI
#   16686 — Jaeger tracing UI
#   4317  — OTLP gRPC collector

services:

  # ── PostgreSQL (batlehub) ─────────────────────────────────────────────────
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: batlehub
      POSTGRES_USER: batlehub
      POSTGRES_PASSWORD: changeme
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U batlehub" ]
      interval: 5s
      timeout: 5s
      retries: 10
    user: "postgres"

  # ── RustFS (S3-compatible object storage) ────────────────────────────────────
  rustfs:
    image: rustfs/rustfs:latest
    restart: unless-stopped
    security_opt:
      - "no-new-privileges:true"
    environment:
      RUSTFS_ROOT_USER: rustfsadmin
      RUSTFS_ROOT_PASSWORD: rustfsadmin
      RUSTFS_CONSOLE_ENABLE: "true"
      RUSTFS_CONSOLE_CORS_ALLOWED_ORIGINS: "*"
      RUSTFS_ADDRESS: 0.0.0.0:9900
      RUSTFS_CONSOLE_ADDRESS: 0.0.0.0:9901
      RUSTFS_OBS_LOG_DIRECTORY: /logs
      RUSTFS_VOLUMES: /data
      # ── OIDC (Authentik but does not work !!) ────────────────────────────────────────────────────
      RUSTFS_IDENTITY_OPENID_ENABLE: on
      RUSTFS_IDENTITY_OPENID_CONFIG_URL: "http://authentik-server:9000/application/o/rustfs/.well-known/openid-configuration"
      RUSTFS_IDENTITY_OPENID_CLIENT_ID: "rustfs"
      RUSTFS_IDENTITY_OPENID_CLIENT_SECRET: "rustfs-secret"
      RUSTFS_IDENTITY_OPENID_GROUPS_CLAIM: "groups"
      RUSTFS_IDENTITY_OPENID_ROLES_CLAIM: "roles"
      RUSTFS_IDENTITY_OPENID_REDIRECT_URI: "http://localhost:9901/oauth_callback"
      RUSTFS_IDENTITY_OPENID_SCOPES: "openid,profile,email,groups"
      RUSTFS_IDENTITY_OPENID_DISPLAY_NAME: "Authentik"
    depends_on:
      authentik-server:
        condition: service_healthy
    user: "1000:1000" # run as non-root to avoid permission issues with mounted volumes
    ports:
      - "9900:9900"
      - "9901:9901" # RustFS console UI
    volumes:
      - ./tmp/rustfs/data:/data
      - ./tmp/rustfs/logs:/logs
    # command: |
    #   sh -c "
    #     mkdir -p /data/rustfs
    #     mkdir -p /data/logs
    #     chown -R 1000:1000 /data
    #     server
    #   "
    healthcheck:
      test: [ "CMD-SHELL", "curl -sf http://localhost:9900/health || exit 1" ]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  # ── RustFS bucket initialiser (runs once, then exits) ────────────────────────
  # rustfs-init:
  #   image: rustfs/rc:latest
  #   restart: "no"
  #   user: "1000:1000"
  #   entrypoint: >
  #     /bin/sh -c "
  #       rc alias set local http://rustfs:9900 rustfsadmin rustfsadmin &&
  #       rc bucket create --ignore-existing local/artifacts &&
  #       echo 'bucket ready'
  #     "

  # ── Authentik (OIDC provider) ─────────────────────────────────────────────────
  # UI: http://localhost:9000  (akadmin / akadmin)
  # ⚠  Credentials below are for LOCAL development only.
  authentik-postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    user: "postgres"
    environment:
      POSTGRES_DB: authentik
      POSTGRES_USER: authentik
      POSTGRES_PASSWORD: authentik
    volumes:
      - authentik_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U authentik" ]
      interval: 5s
      timeout: 5s
      retries: 5

  authentik-redis:
    image: redis:7-alpine
    restart: unless-stopped
    command:
      - redis-server
      - --save
      - ""
      - --appendonly
      - "no"
      - --stop-writes-on-bgsave-error
      - "no"
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 5

  authentik-server:
    image: ghcr.io/goauthentik/server:2026.2.2
    restart: unless-stopped
    command: server
    environment:
      AUTHENTIK_REDIS__HOST: authentik-redis
      AUTHENTIK_POSTGRESQL__HOST: authentik-postgres
      AUTHENTIK_POSTGRESQL__USER: authentik
      AUTHENTIK_POSTGRESQL__NAME: authentik
      AUTHENTIK_POSTGRESQL__PASSWORD: authentik
      AUTHENTIK_SECRET_KEY: batlehub-local-insecure-secret-key
      AUTHENTIK_BOOTSTRAP_PASSWORD: akadmin
      AUTHENTIK_BOOTSTRAP_TOKEN: akadmin-token
    ports:
      - "9000:9000"
    depends_on:
      authentik-postgres:
        condition: service_healthy
      authentik-redis:
        condition: service_healthy
    volumes:
      - ./deploy/auth/batlehub.yaml:/blueprints/custom/batlehub.yaml:ro
    healthcheck:
      test: [ "CMD-SHELL", "curl -sf http://localhost:9000/-/health/live/ || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  authentik-worker:
    image: ghcr.io/goauthentik/server:2026.2.2
    restart: unless-stopped
    command: worker
    environment:
      AUTHENTIK_REDIS__HOST: authentik-redis
      AUTHENTIK_POSTGRESQL__HOST: authentik-postgres
      AUTHENTIK_POSTGRESQL__USER: authentik
      AUTHENTIK_POSTGRESQL__NAME: authentik
      AUTHENTIK_POSTGRESQL__PASSWORD: authentik
      AUTHENTIK_SECRET_KEY: batlehub-local-insecure-secret-key
    depends_on:
      authentik-postgres:
        condition: service_healthy
      authentik-redis:
        condition: service_healthy
    volumes:
      - ./deploy/auth/batlehub.yaml:/blueprints/custom/batlehub.yaml:ro

  # ── Jaeger (OpenTelemetry traces) ─────────────────────────────────────────────
  jaeger:
    image: jaegertracing/all-in-one:1.57
    restart: unless-stopped
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    ports:
      - "4317:4317" # OTLP gRPC
      - "16686:16686" # Jaeger UI

volumes:
  postgres_data:
  authentik_postgres_data:
  authentik_redis_data:
