[package]
name = "batlehub-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "batlehub-cli"
path = "src/main.rs"

[dependencies]
batlehub-core = { path = "../crates/core" }
# Async
tokio = { workspace = true }
# HTTP client
reqwest = { workspace = true, features = ["json", "stream", "multipart", "query"] }
# Stream large downloads to disk
futures = { workspace = true }
# Serialization
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
# Error handling
anyhow = { workspace = true }
# CLI
clap = { workspace = true, features = ["derive", "env"] }
clap_complete = "4"
# Dates
chrono = { workspace = true }
# UUIDs
uuid = { workspace = true }
# TUI
ratatui = "0.30"
crossterm = { version = "0.29", features = ["event-stream"] }
# Text input widget for TUI
tui-input = "0.15"
# Config dir resolution
dirs = "6"
# Pretty table output for plain CLI mode
comfy-table = "8"
# Publish payload encoding (npm attachments, checksums)
base64 = { workspace = true }
ed25519-dalek = "3"
regex = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }
getrandom = "0.3"
# The local gallery proxy (RFC 0011 §4.4): a loopback HTTP server, and the
# `.vsix` it serves for the sign-in entry
actix-web = { workspace = true }
zip = { workspace = true }

[dev-dependencies]
batlehub-web      = { path = "../crates/web" }
batlehub-adapters = { path = "../crates/adapters" }
batlehub-config   = { path = "../crates/config" }
tokio             = { workspace = true }
actix-web         = { workspace = true }
utoipa-actix-web  = { workspace = true }
tempfile          = { workspace = true }
serde_json        = { workspace = true }
reqwest           = { workspace = true, features = ["blocking"] }
zip               = { workspace = true }
mockito           = "1"

[lints]
workspace = true
